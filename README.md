# dotfiles
This repository contains all dotfiles I use for my environment

## Setup

### Prerequisites
1. Clone this repository
2. `cd` into the root of this project
3. Run `bash install.sh`

This will create symbolic links for the configuration files in this repo to the correct locations on your file system.

Note: Git configuration (user name and email) is handled by the ansible playbook in the environment-setup repo.

